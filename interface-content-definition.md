# 勺子星人 App：界面内容定义

## 1. 内容方向修正

当前产品语言策略：

* **勺子**：只作为水豚 IP 的核心道具和启动页世界观介绍，不强行进入日常功能文案。
* **水豚**：负责陪伴感、识别度和情绪稳定感。
* **状态词**：负责用户共鸣，必须普适、直接、像用户自己会说的话。
* **今日声明**：负责把用户选的状态词、天气和时间语境整理成一段“这说的就是我”的话。

一句话原则：**勺子负责记忆点，状态词负责共鸣，声明负责被接住。**

### 1.1 当前设计稿落地口径

当前以 Figma 节点 `323:16045` 为实现基准。主导航固定为三栏：

* `今天`：状态收集与今日声明共用同一入口。
* `抽屉`：历史记录，不叫“我的特卖”。
* `我的`：偏好、数据、隐私、安全说明。

最终可用素材统一从 `assets/app-final` 读取，iOS 工程同步到 `ios/Spoonie/Spoonie/Resources/AppAssets`。状态 IP 使用 `frames` 帧序列，日子抽屉卡片使用紫色状态剪影。

---

## 2. 启动页 / 新手介绍

### 2.1 页面目的

只在第一次打开或用户主动查看时解释“勺子”和水豚的关系。不要在日常主流程反复教育用户。

### 2.2 内容结构

* 主视觉：抱着大勺子的水豚。
* 标题：`勺子星人`
* 说明文案：

```text
有些日子，力气不是源源不断的。

水豚抱着一把勺子，不是提醒你要计算自己，
只是想说：没力气这件事，也可以被认真对待。
```

* 按钮：`今天先放在这里`

### 2.3 禁止

* 不解释过多理论。
* 不出现“你每天有几把勺子”的计算式引导。
* 不把启动页做成心理科普页。

---

## 3. 首页：状态收集

### 3.1 页面目的

让用户用几个真实状态词表达今天，不需要长文、不需要分类、不需要解释。

### 3.2 信息顺序

1. 时间和天气口语化描述。
2. 抱勺水豚。
3. 引导语。
4. 流式状态词面板。
5. 主按钮：生成今日声明。

### 3.3 天气内容

天气要获取，但它是氛围上下文，不是功能主角。

#### 显示规则

* 显示口语化天气，不显示硬数据。
* 默认使用城市级位置。
* 用户拒绝位置时，允许手动城市或无天气模式。

#### 示例

* `天灰蒙蒙的，像罩了层毛玻璃`
* `今天外面风有点硬`
* `空气闷闷的，像怎么都散不开`
* `这个点还醒着，夜晚已经变得很长`

### 3.4 引导语

默认：

```text
今天像什么样子？
```

深夜：

```text
这个点还醒着，身体和脑子是什么状态？
```

雨天：

```text
外面这样，今天更像哪几个词？
```

长时间未打开：

```text
你来不来都可以。今天如果想放一下，也可以。
```

### 3.5 流式状态词面板

状态词不能像传统筛选器或问卷选项。它应该像漂浮的词、纸片、气泡或水面浮叶。

#### 行为规则

* 初始展示 12-18 个状态词。
* 不使用严格宫格。
* 不使用明显 Tab 切换。
* 支持多选，建议 1-5 个。
* 选中后，词语轻轻聚拢到“今天的状态带”或靠近水豚。
* 不显示任务式进度，如 `已选择 3/5`。

#### 视觉语气

* 生理状态可以更钝、更沉。
* 心理状态可以更轻、更散。
* 环境感受可以更雾、更淡。
* 关系压力可以更靠边，不要过度突出。

---

## 4. 状态词库

### 4.1 生理状态

* `躺了一天`
* `没洗头`
* `嗜睡`
* `胸口闷`
* `没有食欲`
* `沉迷刷手机`
* `身体很沉`
* `睡得很碎`
* `头有点胀`
* `什么都懒得弄`

### 4.2 心理状态

* `不想回消息`
* `觉得委屈`
* `无故流泪`
* `害怕明天`
* `随便吧`
* `正在发呆`
* `脑子停不下来`
* `什么都不想管`
* `心里空空的`
* `一点小事就想哭`

### 4.3 环境感受

* `外面太吵`
* `天气闷闷的`
* `房间很乱`
* `被消息淹没`
* `什么都很刺耳`
* `空气很重`
* `灯光太亮`
* `今天太湿了`

### 4.4 关系压力

* `不想解释`
* `怕别人失望`
* `装正常好累`
* `不想见人`
* `被一句话刺到`
* `说不出口`
* `怕打扰别人`
* `不知道怎么回`

### 4.5 状态词禁区

* 不用产品隐喻：`勺子已透支`、`只剩半把勺子`
* 不用诊断词：`抑郁发作`、`焦虑障碍`
* 不用鸡血词：`突破自己`、`重新开始`
* 不用过度网感黑话，除非目标用户明确高频使用。

---

## 5. 今日声明页

### 5.1 页面目的

让用户看到：这段话是基于“我刚才选的词”生成的，而不是泛泛安慰。

### 5.2 信息顺序

1. 今日状态带。
2. 今日声明。
3. 操作区。

### 5.3 今日状态带

格式：

```text
今天的你：躺了一天、胸口闷、不想回消息
```

规则：

* 展示用户刚刚选择的词。
* 最多直接展示 5 个，超出后折叠。
* 不翻译、不改写、不打分。

### 5.4 今日声明

#### 示例

```text
今天已经很沉了。躺了一天、胸口闷、不想回消息，这些不是你在偷懒，而是身体和心里都在把门关小一点。外面灰灰的，你也不用急着把自己解释清楚。
```

#### 生成规则

* 必须自然包含至少 2 个用户选择的状态词。
* 可以带到天气和时间，但不要强行归因。
* 不提供建议。
* 不说“明天会更好”。
* 不使用勺子隐喻。

### 5.5 操作

* 主按钮：`今天先这样`
* 次按钮：`换一种说法`
* 低调入口：`编辑刚才的词`

### 5.6 记录逻辑

* 第一版今日声明生成成功后，默认创建今日记录。
* 用户点击 `换一种说法`，新声明替换当前记录。
* 用户不需要点击“保存”。
* 记录失败时只给轻提示，不打断用户阅读。

---

## 6. 日子抽屉 / 历史记录

### 6.1 页面目的

安放过去的状态，不做成打卡、不做成成绩单。

### 6.2 单条记录结构

* 日期
* 天气小注
* 状态词
* 今日声明

### 6.3 示例

```text
4月16日
天灰蒙蒙的

躺了一天 / 不想回消息 / 胸口闷

今天已经很沉了。你不是没有努力，只是身体和心里都在把门关小一点。
```

### 6.4 禁止

* 不显示连续记录天数。
* 不显示漏记。
* 不显示情绪分数。
* 不推送“你已经很久没记录了”。

---

## 7. 已确认视觉方向

当前核心界面采用“清爽、留白、大 IP、少装饰”的 App 视觉方向。

### 7.1 关键词

* 浅薰衣草雾感背景。
* 大面积留白，页面不要被卡片和图案塞满。
* 抱勺水豚是视觉主角，尺寸要大，表情安静，不卖萌过度。
* 装饰元素只保留少量：小花、星星、白色圆点、浅色雾条、沙漏等。
* 状态词像漂浮的气泡或纸片，不做传统问卷宫格。
* 主按钮使用稳定的紫色，但全屏不能变成高饱和紫色海报。

### 7.2 禁止

* 不要彩纸、烟花、徽章、等级、连续打卡等兴奋型视觉。
* 不要复杂多层卡片，不要把每个模块都框起来。
* 不要写实 3D 或厚重插画。
* 不要让勺子进入功能命名或状态词。
* 不要把页面做成心理测评、效率工具或任务面板。
