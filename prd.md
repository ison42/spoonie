# 产品需求文档 (PRD)：勺子星人 Spoonie

## 1. 文档概述

* **项目名称：** 勺子星人 (Spoonie)
* **产品代号：** 柔和注视
* **产品形态：** 独立 App 优先，先覆盖 iOS，可根据验证结果扩展 Android / 桌面小组件 / 微信小程序轻入口
* **一句话定位：** 给低能量日子的情绪安全屋：用户只需要点选几个真实状态，App 就把今天说成一段容易共鸣、可以被接住的话。
* **产品愿景：** 为低能量、高内耗、慢性疲惫和情绪恢复期人群打造无压力陪伴工具。通过抱着勺子的水豚 IP、灵动的状态收集、天气感知和大模型共情，帮助用户看见那些说不清的身体和心理状态，并把它们温柔地记录下来。

### 1.1 本版本目标

本 PRD 用于指导 MVP 落地，重点解决四件事：

1. 验证“低能量情绪接纳”是否有真实使用需求。
2. 验证“抱勺水豚 IP + 普适状态词 + 口语化今日声明”是否能形成记忆点。
3. 建立足够安全的 AI 情绪支持边界，避免误导为医疗诊断或心理治疗。
4. 明确设计表达标准，让产品从第一眼就传递“低压力、被允许、不催促”的感觉。

### 1.2 非目标

* 不做心理诊断、心理治疗、危机干预或医疗建议。
* 不做自律打卡、连续签到、能量管理 KPI 或效率提升工具。
* 不把“勺子理论”强行写进日常功能文案，避免用户理解成本和隐喻疲劳。
* 不以恋爱型 AI 伴侣为核心，不制造强依赖关系。
* MVP 阶段不做复杂社区，避免脆弱用户互相触发和内容治理成本失控。

---

## 2. 市场与机会判断

### 2.1 需求背景

用户侧存在持续增长的轻量情绪支持需求：

* 中国用户对压力、焦虑、睡眠和低能量状态的表达越来越日常化，但正式心理咨询仍有门槛：贵、重、羞耻感强、需要预约。
* 大量用户并不想被“治好”，只想在某个低能量瞬间被理解、被允许、被温柔地接住。
* 情绪疗愈消费正在从标准化服务转向个性化、生活化、内容化表达，例如 MBTI、寺庙、演唱会、助眠、AI 陪伴、虚拟 IP 等。
* 独立 App 更适合承载稳定的情绪空间、系统级通知、桌面组件和更完整的隐私控制；微信小程序可作为后续低门槛获客入口，而不是首发主形态。

### 2.2 机会切口

市场已有冥想、睡眠、心理测评、AI 聊天、情绪日记等产品，但仍存在空位：

| 赛道 | 常见产品感受 | 用户压力 | 勺子星人机会 |
| --- | --- | --- | --- |
| 冥想/睡眠 | 专业、课程化、需要练习 | 中等 | 不要求练习，只接纳当下状态 |
| 情绪日记 | 记录、分析、复盘 | 中等偏高 | 不要求写长文，用标签即可 |
| 心理咨询 | 正式、昂贵、强目标 | 高 | 明确不治疗，只做日常安放 |
| AI 伴侣 | 连续聊天、拟人关系强 | 依赖风险高 | 单次今日声明 + 轻陪伴，降低关系绑定 |
| 自律打卡 | 成长、坚持、量化 | 高 | 明确反打卡，断更天然合理 |

### 2.3 目标市场判断

**判断：有市场，但应定位为“轻量情绪疗愈/低能量陪伴工具”，而不是“心理健康医疗 App”。**

理由：

* 轻量情绪支持需求足够普遍，尤其集中在年轻用户、城市用户、学习/工作压力人群。
* App 形态适合验证更深的复访、私密记录和视觉陪伴体验；早期可用 TestFlight / 内测包验证内容调性，再进入公开分发和备案流程。
* 产品具有清晰差异化：不是让用户变好，而是让用户停止责怪自己。
* 风险也明确：心理健康相关产品必须处理安全边界、隐私、未成年人、危机识别和 AI 依赖问题。

### 2.4 首发目标用户

MVP 优先服务一个核心人群，其他人群作为观察对象。

#### 核心用户：城市高内耗年轻女性

* 年龄：18-35 岁。
* 状态：学习、工作、人际关系、亲密关系或家庭期待带来的长期内耗。
* 典型表达：“我也没干什么但就是很累”“不想回消息”“洗头都像一个项目”“今天只想躺着”。
* 需求：不想被教育，不想被安排任务，只想被接纳、被轻轻命名。

#### 次级用户：慢性疲惫/亚健康/慢病相关低能量人群

* 状态：体力波动明显，对“今天什么都做不了”有更深负罪感。
* 需求：需要一种非病理化、非鸡血的语言来描述消耗。

#### 观察用户：情绪恢复期用户

* 状态：可能曾经咨询、治疗或自助学习过心理健康知识。
* 需求：日常低谷时需要低门槛辅助，但不希望产品替代专业支持。

---

## 3. 产品原则与世界观

### 3.1 核心世界观：勺子只属于 IP，不压到功能上

“勺子理论 (Spoon Theory)”只作为品牌世界观和水豚 IP 的底层隐喻，不作为日常功能的强制语言。

产品启动页或新手介绍可以轻轻解释：

> 有些人的力气不是源源不断的，今天能做的事，可能只有很少很少。水豚抱着一把勺子，不是提醒你要计算能量，而是在说：没力气这件事，也可以被认真对待。

日常使用中，用户看到的应该是普适、直接、真实的状态词，例如“躺了一天”“不想回消息”“胸口闷”“害怕明天”，而不是“勺子已透支”“今天只有半把勺子”这类需要理解隐喻的表达。

**原则：勺子负责记忆点，状态词负责共鸣。**

### 3.2 产品价值观

* **不催促：** 不说“加油”“振作”“你可以更好”。
* **不评判：** 不把躺下、断更、已读不回解释为失败。
* **不量化羞辱：** 不显示连续天数、排名、完成率、健康分。
* **不替代人：** AI 可以陪用户停一下，但不假装是医生、咨询师或唯一支持。
* **轻而可信：** 语气柔软，但安全、隐私和边界必须坚硬。

### 3.3 情绪体验关键词

用户打开产品时应该感到：

* “这里不会催我。”
* “我不用解释那么多。”
* “它说出来的就是我今天的样子。”
* “我可以随时来，也可以随时消失。”

---

## 4. 视觉与设计表达规范

### 4.1 设计总原则

产品的视觉目标不是“可爱”，而是“安全”。可爱只是入口，安全才是留存。

设计表达遵循：

1. **低刺激：** 避免高饱和、大对比、强动效、频繁弹窗。
2. **低任务感：** 不出现进度条、任务卡、目标看板、成就徽章。
3. **低社交压力：** 不出现在线状态、公开分享广场、点赞评论。
4. **高被允许感：** 文案、留白、状态变化都要表达“这样也可以”。
5. **高触感：** 背景、卡片、按钮像软布、雾气、木勺、纸张，而不是效率工具。

### 4.2 核心 IP：抱勺水豚

水豚是情绪稳定的化身，勺子是它随身携带的核心识别物。勺子不需要在功能文案中反复解释，只需要在视觉上形成“这个 App 的陪伴者”的记忆。

#### 默认状态

* 水豚抱着一把和它差不多高的巨大木勺，安静发呆。
* 眼神半闭，不盯着用户，不制造“被审视”的压力。
* 身体轮廓圆润但不要过度幼态，避免显得像儿童产品。

#### 状态联动矩阵

水豚状态跟随用户选择的真实状态词变化，但不把状态翻译成“勺子语言”。

| 用户选择 | 水豚状态 | 设计意图 |
| --- | --- | --- |
| 躺了一天 / 嗜睡 | 水豚趴在软垫上，勺子横放在身边 | 允许停机 |
| 没洗头 / 没有食欲 | 水豚裹着小毯子，勺子像靠枕 | 接住身体低能量 |
| 胸口闷 / 无故流泪 | 水豚把勺子抱在胸前，眼睛低垂 | 接住脆弱感 |
| 不想回消息 / 不想见人 | 水豚半躲在勺子后面 | 尊重退缩和边界 |
| 害怕明天 / 觉得委屈 | 水豚坐在微暗环境里，勺子像小灯旁的影子 | 外界先替用户背一点 |
| 随便吧 / 正在发呆 | 水豚放空，勺子轻轻靠着它 | 不急着定义状态 |

### 4.3 色彩 Tokens

整体采用马卡龙扁平矢量风格，但需要避免全局过甜、过儿童化。

| Token | 色值 | 用途 |
| --- | --- | --- |
| `env-night-heavy` | `#526071` | 夜晚、雨天、躲进被窝 |
| `env-comfort-sand` | `#FCE3CD` | 晴天、温暖、轻微恢复 |
| `env-anxiety-purple` | `#D5CBE8` | 内耗、焦虑、神经紧绷 |
| `env-calm-green` | `#CDEBCE` | 平静、轻微有力气 |
| `env-mist-blue` | `#D7E6EE` | 阴天、雾气、低对比背景 |
| `env-soft-rose` | `#F3D4D8` | 委屈、脆弱、需要抱抱 |
| `ink-primary` | `#4A4F55` | 主文本 |
| `ink-secondary` | `#737A82` | 次级文本 |
| `surface-soft` | `rgba(255, 255, 255, 0.62)` | 毛玻璃卡片 |
| `surface-chip-default` | `rgba(255, 255, 255, 0.50)` | 未选中标签 |
| `surface-chip-selected` | `rgba(255, 255, 255, 0.84)` | 选中标签 |

#### 色彩使用规则

* 单屏最多出现 2 个主环境色，避免像调色盘。
* 用户低落时不强行使用明亮鼓励色。
* 夜晚模式不做纯黑，使用蓝灰和暖白文字，像关灯后的房间。
* 危机提示必须使用清晰但不刺眼的提示色，不用红色大面积警告。

### 4.4 字体与排版

* 中文优先使用系统字体，确保 iOS / Android 内渲染稳定。
* 正文行高建议 `1.7-1.9`，营造“慢慢说”的感觉。
* 首页标题不使用大喊式字号，避免像营销页。
* AI 回复文字最大宽度控制在舒适阅读范围内，避免一整行铺满。
* 不使用密集表单，不出现超过 2 层的信息层级。

### 4.5 动效规范

动效应该像“呼吸”，不应该像“奖励反馈”。

* 水豚默认只有极轻微呼吸动效，周期 4-6 秒。
* 天气元素缓慢漂移，不出现快速粒子或大面积闪烁。
* AI 打字机速度偏慢，但必须提供“直接显示全文”兜底，避免急性焦虑用户被迫等待。
* 状态切换使用淡入淡出和轻微位移，不使用弹跳。
* 不使用连续弹窗、烟花、徽章爆开等兴奋型反馈。

### 4.6 文案语气

文案要像一个坐在旁边陪用户发呆的朋友，而不是教练、老师、咨询师或客服。

#### 可以说

* “今天像什么样子？”
* “选几个就好，不用解释。”
* “先把今天放在这里。”
* “外面太吵了，躲一下是合理的。”
* “没有回复，也是一种保护。”
* “你现在这样，也可以被记录。”

#### 不要说

* “坚持记录会更了解自己。”
* “明天一定会更好。”
* “试试完成一个小目标。”
* “你需要改变思维方式。”
* “检测到你的负面情绪较高。”

### 4.7 设计上建议新增/调整

1. **首屏引导语使用真实状态提问。**
   * 默认：“今天像什么样子？”
   * 深夜：“这个点还醒着，身体和脑子是什么状态？”
   * 雨天：“外面这样，今天更像哪几个词？”
   * 长时间未打开：“你来不来都可以。今天如果想放一下，也可以。”

2. **状态词要普适、直接、可秒懂。**
   * 生理状态：`躺了一天`、`没洗头`、`嗜睡`、`胸口闷`、`没有食欲`、`沉迷刷手机`
   * 心理状态：`不想回消息`、`觉得委屈`、`无故流泪`、`害怕明天`、`随便吧`、`正在发呆`
   * 环境感受：`外面太吵`、`天气闷闷的`、`房间很乱`、`被消息淹没`、`什么都很刺耳`
   * 关系压力：`不想解释`、`怕别人失望`、`装正常好累`、`不想见人`

3. **收集面板要流式、灵动，不做常规表单。**
   * 状态词像漂浮的轻标签、气泡、纸片或水面浮叶，大小和位置有轻微差异。
   * 选项不按严格宫格排列，避免工具感和测评感。
   * 生理/心理/环境可以通过空间层次、颜色温度、轻微聚类来区分，但不需要明显 Tab。
   * 用户选中后，词语可以轻轻靠近水豚或汇入“今天的状态带”，形成被收集的感觉。
   * 不做强烈弹跳、打分、进度条或“已选择 3/3”的任务式反馈。

4. **反馈态必须展示用户刚刚选择的词。**
   * 今日声明上方先展示“今天的你：躺了一天、胸口闷、不想回消息”。
   * 再展示 AI 生成的今日声明。
   * 这样用户能确认：这段话确实是从自己选的状态长出来的，而不是一段泛泛安慰。

5. **今日声明默认记录。**
   * 用户生成声明后，系统自动把当天状态和第一版声明保存到记录。
   * 用户可以点击 `换一种说法`，新版本替换当前记录中的声明。
   * 不再要求用户额外点击“保存”，避免把情绪表达变成任务。

---

## 5. 功能模块详细设计

### 5.1 模块一：首页 - 状态收集态

#### 页面目标

让用户用 5 秒点选几个“今天真实发生在自己身上的状态”，不需要写作、不需要解释、不需要登录感知。

#### Figma v1 对齐说明

当前设计稿节点 `323:16045` 将主流程定义为 375 x 812 的 iOS 竖屏体验，底部固定三栏：`今天`、`抽屉`、`我的`。`今天`页内包含两个状态：未生成时为状态收集，生成后切到今日声明；`抽屉`承载历史记录；`我的`承载个人设置和数据安全入口。

设计稿中的旧占位命名如 `我的特卖` 只视为图层残留，产品文案统一为 `抽屉`。底部 tab 文案统一为：

| Tab | 页面 | 作用 |
| --- | --- | --- |
| `今天` | 状态收集 / 今日声明 | 今天的状态输入和声明结果 |
| `抽屉` | 日子抽屉 | 自动保存的历史声明 |
| `我的` | 个人中心 | 偏好、隐私、数据、关于勺子 |

#### 页面结构

* **Top：人情味环境感知**
  * 显示极简时间，如 `14:20`。
  * 获取天气、时间、城市模糊位置，并转化成口语化环境描述。
  * 示例：`今天外面风呼呼的`、`天灰蒙蒙的，像罩了层毛玻璃`、`这个点还醒着，已经很辛苦了`。
  * 若用户拒绝位置权限，使用手动城市或无天气模式，不阻断主流程。

* **Middle：陪伴区**
  * 展示抱勺水豚。
  * 根据已选状态词实时切换水豚姿态，但不展示“勺子数量”。
  * 引导语使用设计稿文案 `今天的你还好吗？`，副文案为 `选选几个词，看看今天的你`。
  * 头部使用透明底装饰图 `collect_header_square_bg_true_transparent.png`，水豚 IP 使用本地帧序列动画，默认状态为抱勺静坐。

* **Bottom：流式状态词面板**
  * 状态词以非规则流式布局出现，不做传统宫格。
  * 首屏展示 7 个高共鸣状态词，位置、角度、宽度不完全一致，形成设计稿里的轻微漂浮感。
  * 默认选中示例为 `躺了一天`、`胸口闷`、`不想回消息`；未选中示例为 `没洗头`、`觉得委屈`、`正在发呆`、`害怕明天`。
  * 支持多选，建议 1-5 个；不强制上限提示，超出时自然弱化未选词。
  * 保留一个轻输入入口：`写点其他`，以白色胶囊按钮呈现，不突出。
  * 主操作：`生成今日声明`。

#### 状态词生成规则

* 默认展示：生理状态 + 心理状态 + 环境感受 + 关系压力混合。
* 天气影响：雨天/闷热/大风/降温会影响环境词和声明上下文，但不把疲惫硬说成“天气扣勺子”。
* 时间影响：深夜增加“睡不着”“害怕明天”“脑子停不下来”；早晨增加“起不来”“身体很沉”。
* 历史影响：若用户常选某类词，可适度前置，但不做“你最近总是”式判断。
* 语言规则：词语必须像真实用户会说的话，不用产品黑话、心理学术语或勺子隐喻。

#### MVP 状态词库

| 分类 | 词语 |
| --- | --- |
| 生理状态 | `躺了一天`、`没洗头`、`嗜睡`、`胸口闷`、`没有食欲`、`沉迷刷手机`、`身体很沉`、`睡得很碎` |
| 心理状态 | `不想回消息`、`觉得委屈`、`无故流泪`、`害怕明天`、`随便吧`、`正在发呆`、`脑子停不下来`、`什么都不想管` |
| 环境感受 | `外面太吵`、`天气闷闷的`、`房间很乱`、`被消息淹没`、`什么都很刺耳`、`空气很重` |
| 关系压力 | `不想解释`、`怕别人失望`、`装正常好累`、`不想见人`、`被一句话刺到`、`说不出口` |

### 5.2 模块二：首页 - 抚慰反馈态

#### 页面目标

把用户刚刚选择的状态词、天气和时间语境组合起来，生成一段容易引起共鸣的“今日声明”。

#### 输入变量

* `weather_conversational`：口语化天气。
* `selected_terms`：用户选择的状态词。
* `selected_groups`：状态词所属分类，如生理/心理/环境/关系。
* `time_context`：早晨/午后/傍晚/深夜。
* `recent_context_optional`：最近几次高频标签，只用于温柔连贯，不用于诊断。

#### 输出内容

* 页面标题：`今日声明`。
* 日期副标题：如 `5月24日 星期日`。
* 天气胶囊：如 `广州 · 微风有雾`，位于标题区右侧。
* 主视觉：根据本次主要状态切换水豚 IP，本地资源使用对应 `frames_clean` 帧序列；默认示例为裹毯抱勺。
* 状态带：卡片内先展示 `今天的你`，再展示用户刚刚选择的状态词，例如 `躺了一天`、`胸口闷`、`不想回消息`。
* 正文卡片：不使用“分析结果”，展示 `今日声明` 内容。
* 正文：80-140 字。
* 风格：朋友聊天、轻、慢、不催促。
* 操作：
  * 当前设计稿主入口为 `换换今天的状态`，回到状态收集态。
  * 后续版本可补充 `换一种说法`，用于替换当前记录的声明。

#### 记录逻辑

* 生成第一版今日声明后，系统自动创建当天记录。
* 用户点击 `换一种说法` 后，新声明覆盖当前记录中的 `active_statement`，旧版本可在本地短暂缓存，MVP 不必展示版本历史。
* 用户点击 `编辑刚才的词` 后，回到状态收集态；重新生成后更新当天记录。
* 若生成失败，仍保存用户选择的状态词和天气上下文，声明字段为空，后续可重试。

#### 状态

* 生成中：水豚慢慢把选中的词收拢到身边，文案为 `我在把这些词慢慢放成一句话`。
* 失败：`刚刚这段话没有端稳，我们再轻轻试一次。`
* 网络弱：`信号有点散，今天也可以先不用生成。`

### 5.3 模块三：今日记录 / 日子抽屉

#### 页面目标

自动保存那些“不用解释的日子”，给用户一个低压力回看空间。

#### 结构

* 顶部标题：`日子抽屉`。
* 副标题：`每个日子，都是你认真过的证据`。
* 头部视觉：透明底抽屉装饰 + 抽屉守护水豚动图，右侧摆放，降低页面重复感。
* 轻提示胶囊：`已连续记录 12 天`。该信息只作为“这些日子被收好”的反馈，不做成成就、排名或任务压力。
* 历史卡片：
  * 日期：如 `4月16日 周日`。
  * 天气小注：如 `广州 · 微风有雾`。
  * 状态词：显示 2-5 个小标签，超出时折叠。
  * 正文：AI 生成的今日声明摘要。
  * 右侧状态剪影：使用本次主要情绪对应的紫色低透明 IP 剪影，避免和顶部完整 IP 重复。

#### 明确禁止

* 不把连续记录做成目标、积分、奖励或断签惩罚。
* 不显示本周完成率。
* 不做情绪曲线排名。
* 不推送“你已经很久没记录了”。
* 不把空白日期标红或置灰。

### 5.4 模块四：个人中心

#### 页面目标

承载用户偏好、隐私和数据安全入口，不让设置页看起来像冷冰冰的系统后台。

#### MVP 结构

* 顶部：`我的`，展示小型抱勺水豚和一句低压力说明。
* 今日偏好：天气城市、声明语气、提醒时间。
* 记录与数据：导出日子抽屉、删除全部记录、本地缓存。
* 安全与说明：AI 非医疗声明、危机帮助入口、隐私政策、关于勺子。
* 反馈：问题反馈、版本信息。

#### 设计要求

* 延续浅紫雾面背景、白色圆角卡片和紫色重点色。
* 设置项使用轻图标，不用高对比警告样式。
* 危机帮助入口需要清晰可见，但不使用大面积红色。

#### 可选洞察

在用户累计 7 条以上记录后，可出现“轻洞察”，但必须使用非评判语气：

* “最近让你变沉的，常常是回消息。”
* “雨天好像更容易把你按暂停。”
* “你不是每天都这样，只是有些日子真的更重。”

### 5.5 模块五：设置与安全

MVP 也必须具备：

* 隐私说明。
* 数据删除入口。
* AI 非医疗声明。
* 危机帮助入口。
* 位置权限说明与关闭入口。
* 未成年人保护说明。

---

## 6. AI 提示词工程规范

AI 引擎的成败在于能否把用户选择的普通状态词，组织成一段“像是在说我”的今日声明。

### 6.1 System Prompt 草案

```text
你是一个擅长把日常低能量状态说清楚的陪伴型写作者。你的任务不是治疗用户，也不是鼓励用户变好，而是基于用户选择的状态词，写一段容易产生共鸣的「今日声明」。

输入变量：
- 口语化天气状态：{weather_conversational}
- 用户选择的状态词：{selected_terms}
- 状态词分类：{selected_groups}
- 时间语境：{time_context}
- 可选历史语境：{recent_context_optional}

请结合上述信息，写一段 80-140 字的「今日声明」。

必须严格遵守：
1. 禁止爹味与催促：不许说“振作起来”“加油”“明天会更好”“你可以试试”。
2. 禁止行动建议：不提供任务、练习、计划或自我提升建议。
3. 必须自然带到用户选择的至少 2 个状态词，不能只写泛泛安慰。
4. 天气和时间只作为氛围上下文，不能强行归因，也不能使用冷硬数据。
5. 语言要普通、具体、容易懂，像朋友轻轻复述今天发生了什么。
6. 不要使用“勺子”“能量值”“情绪分数”等产品隐喻，除非用户自己输入。
7. 不诊断，不分析人格，不说“你是焦虑型/抑郁状态”等判断。
8. 不制造依赖：不要说“只有我懂你”“你只需要我”。
9. 遇到自伤、自杀、伤害他人、严重危机内容时，不生成普通今日声明，改用危机回应模板。
```

### 6.2 危机回应模板

当用户输入或自定义文本中出现自伤、自杀、伤害他人、明确求死、具体计划等风险信号时：

* 停止普通今日声明生成。
* 用简短、稳定、非惊吓语气回应。
* 鼓励用户立刻联系身边可信任的人或当地紧急服务。
* 提供危机热线/紧急资源入口。
* 不进行长篇推理，不承诺保密，不让用户继续只和 AI 对话。

示例语气：

```text
我听见你现在很危险，也很难独自扛住。请先把手机放在能联系人的地方，立刻联系身边可信任的人，或拨打当地紧急电话。你不需要一个人处理这一刻。
```

### 6.3 输出质量标准

合格输出：

* 读起来像朋友，不像心理科普。
* 能自然包含用户选择的状态词。
* 能把用户的疲惫合理化。
* 没有行动要求。
* 没有诊断词。
* 没有过度亲密或唯一陪伴暗示。

不合格输出：

* “建议你早点睡。”
* “可以试试深呼吸。”
* “你的焦虑来源于……”
* “我会一直陪着你，只有我懂你。”
* “坚持记录 21 天会有变化。”
* “你今天的勺子已经清零了。”（除非用户主动选择/输入了勺子相关表达）

---

## 7. 数据与隐私

### 7.1 数据结构

`records` 集合建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | App 匿名 ID 或登录后用户 ID |
| `created_at` | timestamp | 创建时间 |
| `weather_conversational` | string | 口语化天气 |
| `weather_raw_minimal` | object | 最小必要天气数据 |
| `location_city` | string | 城市级位置，不存精确地址 |
| `selected_terms` | array | 用户选择的状态词 |
| `selected_groups` | array | 状态词分类，如生理/心理/环境/关系 |
| `primary_state_asset` | string | 主要状态对应的 IP 资源 key，如 `sleepy_lied_down` |
| `drawer_stamp_asset` | string optional | 日子抽屉卡片右侧剪影资源 |
| `custom_text` | string optional | 用户自定义短文本 |
| `active_statement` | string | 当前生效的今日声明 |
| `statement_versions_count` | number | 今日声明换写次数 |
| `risk_level` | string | `none` / `low` / `high` |
| `model_version` | string | 生成模型版本 |

### 7.2 隐私原则

* 默认只存城市级位置，不存精确经纬度，除非天气服务必须短暂使用。
* 明确告知 AI 生成会处理用户选择的状态词、天气上下文和可选文本。
* 提供一键删除全部记录。
* 不把用户情绪数据用于广告定向。
* 未经用户主动操作，不生成可公开分享的情绪内容。

---

## 8. 技术架构与合规路线

### 8.1 App 技术路线

MVP 当前按用户确认的方向优先做 **iOS 原生 App**，使用 `SwiftUI` 落地。理由是本产品第一阶段最重视视觉情绪、动效、系统权限、通知和本地私密记录，原生实现更容易把页面触感做细。

* **客户端框架：** `SwiftUI`
* **首发设备：** iPhone，竖屏优先，设计基准 375 x 812。
* **资源策略：**
  * 最终确认素材统一放在 `assets/app-final`。
  * iOS 工程内同步到 `ios/Spoonie/Spoonie/Resources/AppAssets`。
  * IP 动图优先使用稳定帧序列 `frames_clean`，避免 WebP/APNG 在不同环境下出现边缘脏点或位置抖动。
  * 状态和素材通过 manifest 映射，不在代码里临时猜图。
* **后续扩展：**
  * 若 MVP 数据验证通过，再评估 Android / Flutter 复刻或跨端重构。
  * 不在第一阶段为了跨端牺牲 iOS 视觉还原。

### 8.2 后端与云服务

独立 App 不能依赖微信小程序内置的 `wx.cloud` 能力，但仍可使用腾讯 CloudBase / Serverless 作为后端，避免一开始自建和运维服务器。

* **后端候选：**
  * 腾讯 CloudBase：数据库、云函数、云存储、身份认证、AI 能力、HTTP API。
  * Supabase：适合海外或非大陆优先版本，但大陆合规和访问稳定性需另评估。
  * 自建云服务器：灵活度最高，但运维、备案、安全和成本压力更大。
* **MVP 推荐：** 腾讯 CloudBase + HTTP API / 移动端 SDK。
* **云函数：**
  * `getWeatherText`：获取天气并转为口语化描述。
  * `generateStatement`：调用大模型生成今日声明。
  * `riskCheck`：对用户输入做轻量风险识别。
  * `upsertTodayRecord`：默认创建或更新当天记录。
  * `deleteUserData`：删除用户全部数据。
* **数据库：**
  * `records`：日子抽屉记录。
  * `user_preferences`：用户偏好，如是否开启位置、是否允许轻洞察。
  * `users`：匿名 ID、登录方式、合规同意版本。

### 8.3 账号与登录

MVP 尽量降低登录压力：

* 默认支持匿名体验，本地生成匿名用户 ID。
* 需要跨设备同步时，再引导 Apple 登录 / 手机号登录 / 微信登录。
* 不在首屏强制登录。
* 用户删除数据后，应同时清除云端记录与本地缓存。

### 8.4 备案与合规准备

若面向中国大陆用户公开上架，需要在 Phase 0/Phase 1 前准备：

* 主体实名：个人或公司；长期商业化建议公司主体。
* 域名：用于 API、官网、隐私政策、用户协议。
* 国内云服务资源：CloudBase / 云函数 / 云托管 / 云存储等。
* App 备案：准备 App 名称、包名、图标、简介、服务类型、域名/IP 等信息。
* ICP 备案：若使用中国大陆域名和服务接入，需要按接入商流程办理。
* 隐私政策与用户协议：必须覆盖位置、情绪标签、AI 生成、数据删除、未成年人保护。
* 生成式 AI 合规：优先调用已备案模型能力，并在产品内公示模型名称、备案号或上线编号；如产品功能需要，按地方网信办要求做应用/功能登记。
* 医疗边界：全链路避免“诊断、治疗、疗效承诺”等表达。

### 8.5 性能要求

* 首页首屏 2 秒内可交互。
* AI 生成 5 秒内应有首字输出或加载状态。
* 失败时保留本地兜底文案，不让用户看到技术错误。
* 低端机优先保证静态体验，动效可降级。

---

## 9. MVP 范围

### 9.1 必须做

* iOS 优先的 App 核心体验。
* 首页天气/时间口语化感知。
* 水豚默认状态 + 至少 4 个状态联动。
* 流式状态词面板。
* AI 今日声明生成。
* 今日声明默认记录。
* 历史列表与详情。
* 匿名体验 + 可选登录。
* 隐私说明、删除数据、AI 非医疗声明。
* 基础危机词识别和危机回应模板。
* App 备案、ICP 备案和 AI 合规资料准备。

### 9.2 暂不做

* 社区、评论、点赞。
* 连续打卡和成就体系。
* 专业量表和诊断报告。
* 语音通话型 AI 陪伴。
* 复杂人格设定。
* 付费墙。

### 9.3 可灰度

* `换一种说法`。
* 轻洞察。
* 自定义状态词。
* 分享为图片，但默认隐藏入口，避免社交压力。
* 水豚主题皮肤。

---

## 10. 验证指标

### 10.1 核心指标

* 首次完成率：进入首页后完成一次今日声明生成的比例。
* 默认记录完成率：生成后成功写入当天记录的比例。
* 次日/7 日回访率：验证是否有自然复访。
* `换一种说法` 点击率：判断 AI 文案是否击中。
* 删除率/关闭位置率：判断隐私压力。

### 10.2 体验指标

通过小样本访谈或问卷验证：

* 用户是否觉得“没有被催促”。
* 用户是否觉得状态词“像我会说的话”。
* 用户是否愿意在低能量日打开。
* 用户是否觉得文案过度亲密、过度幼稚或过度诊断。

### 10.3 成功标准

MVP 上线 2-4 周后，若满足以下条件，可进入下一阶段：

* 首次完成率 >= 55%。
* 默认记录成功率 >= 95%。
* 7 日回访率 >= 15%。
* 用户访谈中，“不催促/被允许/舒服”相关反馈高频出现。
* 无重大 AI 安全事故或隐私投诉。

---

## 11. 商业化假设

MVP 不急于商业化，先验证复访和情绪价值。后续可考虑：

### 11.1 会员订阅

* 更多水豚状态与主题。
* 历史轻洞察。
* 私密主题抽屉。
* 更多语气包：更冷幽默、更温柔、更像朋友、更短句。

### 11.2 单次购买

* IP 装扮。
* 季节主题。
* 导出手账图。

### 11.3 B 端/联名

* 与睡眠、香薰、文创、女性健康、慢病公益组织做轻联名。
* 谨慎选择品牌，避免破坏安全屋感。

### 11.4 不建议的商业化

* 情绪焦虑广告定向。
* 诱导性连续订阅。
* 用“你最近状态很差”推动付费。
* 将用户推向未验证的咨询服务或疗愈课程。

---

## 12. 风险与应对

| 风险 | 表现 | 应对 |
| --- | --- | --- |
| AI 安全风险 | 用户出现危机内容，AI 仍普通安慰 | 风险识别 + 危机模板 + 人工资源入口 |
| 依赖风险 | 用户把产品当唯一支持 | 不做强拟人关系，不说唯一陪伴，不做无限聊天 |
| 定位误解 | 被认为是医疗/心理咨询 | 全链路声明“非医疗、非诊断、非治疗” |
| 隐私担忧 | 用户不愿授权天气/位置 | 支持手动城市或无天气模式 |
| 审美跑偏 | 过甜、过幼、像儿童 App | 控制饱和度、文本成熟、IP 不幼态 |
| 留存不足 | 生成一次后无复访 | 抽屉、轻洞察、天气语境、主题状态增强回看价值 |
| 内容同质化 | AI 回复像模板 | 加入天气、时间、状态词组合与语气包 |

---

## 13. 路线图

### Phase 0：原型验证

* Figma / 静态页面验证首屏感受。
* 20-30 位目标用户测试文案和 IP。
* 验证“抱勺水豚”是否能被记住，以及状态词是否能被用户秒懂。
* 确认 App 技术栈、云服务、主体、域名、备案路径。

### Phase 1：MVP App

* 首页输入。
* AI 今日声明。
* 日子抽屉。
* 隐私与安全基础能力。
* 匿名体验与可选登录。
* iOS 内测分发。

### Phase 2：复访增强

* 轻洞察。
* 更多状态动画。
* 自定义状态词。
* 主题抽屉。

### Phase 3：商业化试探

* 皮肤/主题。
* 会员语气包。
* 联名内容。

---

## 14. 参考依据

以下资料用于支持市场与风险判断，后续若进入融资、汇报或正式立项，需要进一步补充更完整的竞品数据和用户访谈证据。

* WHO 中国心理健康主题页：抑郁和焦虑是中国常见心理健康问题之一，心理健康服务可及性仍在持续建设。
* 《中国国民心理健康发展报告（2023-2024）》：覆盖多年龄段样本，指出城市户口人群焦虑风险显著高于农村户口人群，大学生中低年级抑郁和焦虑风险高于高年级。
* Grand View Research: China Mental Health Apps Market Outlook：估算中国 mental health apps 市场 2024 年收入约 7.522 亿美元，2030 年预计约 18.685 亿美元。
* 艾瑞咨询《中国情绪疗愈行业研究报告》：情绪疗愈需求呈现个性化、生活化趋势，用户关注睡眠、解压、松弛感等场景。
* AI 伴侣相关行业报道与研究：AI 情绪陪伴增长较快，但存在内容安全、未成年人保护、情感依赖和危机回应风险。

---

## 15. 待确认问题

1. 产品首发用户是否锁定为“城市高内耗年轻女性”？
2. 视觉 IP 是否确定为水豚，还是需要再比较“水豚/海獭/毛毯小人”等方案？
3. 是否接受“非医疗声明”和“危机入口”在设置页与 AI 触发时明确出现？
4. App MVP 是否优先选择 Flutter，还是 React Native / SwiftUI？
5. 天气能力是必须授权位置，还是优先使用手动城市选择？
6. 首发是否只做 iOS，Android 是否进入 Phase 2？
7. 主体备案采用个人还是公司主体？
